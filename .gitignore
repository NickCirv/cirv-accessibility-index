# dependencies
node_modules/

# runtime artifacts (regenerated by `npm run refresh`)
data/
public/
*.db
*.db-shm
*.db-wal

# the public scanner is a separate repo, present locally for dev only
scanner/

# os
.DS_Store
